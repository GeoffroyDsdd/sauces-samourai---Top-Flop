import{NextResponse}from"next/server";import bcrypt from"bcryptjs";import{makeSession}from"@/lib/auth";export async function POST(req:Request){const b=await req.json();const good=b.username===process.env.ADMIN_USERNAME&&await bcrypt.compare(b.password||"",process.env.ADMIN_PASSWORD_HASH||"");if(!good)return NextResponse.json({error:"refusé"},{status:401});const r=NextResponse.json({ok:true});r.cookies.set("smw_admin",await makeSession(),{httpOnly:true,secure:true,sameSite:"lax",maxAge:28800,path:"/"});return r}
